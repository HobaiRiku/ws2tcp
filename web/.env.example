# 复制为 .env 或 .env.local 后按需修改, 这两个文件被 git 忽略.
# Vite 仅暴露以 VITE_ 开头的变量给客户端.

# Vite dev 服务器端口, 默认 5266
VITE_DEV_PORT=5266

# 后端 (Go ws2tcp run) 的 API base URL, dev 模式下 /api 会被 proxy 到这里
VITE_API_BASE=http://127.0.0.1:8080
