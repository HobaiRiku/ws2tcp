package services

// CRUD for ws2tcp client identities (the server-side "users" who present a
// clientId/clientSecret). Backed by config.yaml; persists via the yaml.v3
// Node writer in internal/config. TODO.
